// The className-aware React Native components. They come from
// react-native-css rather than react-native because the global className
// polyfill is disabled — see metro.config.js for why.
export {
  Pressable,
  ScrollView,
  Text,
  View,
} from "react-native-css/components";
